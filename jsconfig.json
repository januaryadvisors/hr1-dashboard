{
  "//": [
    "Editor support only — nothing in the build reads this file.",
    "The project is plain JavaScript (see README, 'Plain JavaScript'), so there",
    "is no compile step to configure. This exists so VS Code resolves the",
    "@typedef blocks the components carry and offers autocomplete on props.",
    "checkJs is off on purpose: the JSDoc is documentation, not a gate."
  ],
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "checkJs": false,
    "noEmit": true,
    "resolveJsonModule": true
  },
  "include": ["src", "scripts", "vite.config.js"],
  "exclude": ["node_modules", "dist"]
}

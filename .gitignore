node_modules/
dist/
.DS_Store
*.log
.env
.env.local

# Geometry + fixtures are generated by `npm run build:data` but COMMITTED so CI
# builds standalone (see README). Source geojson is not committed — it's 8MB.
data-src/
